# 前端卡资源本地化：服务端插件技术简述

## 1. 项目目标与结论

本项目解决的是前端卡依赖在线图床的问题：扫描角色卡、消息及 iframe 内的在线图片、音频、视频资源，将其下载到酒馆本地，并把原始 URL 自动改写为本地资源路由。

结论很明确：**前端扩展适合做界面、扫描、队列和资源替换；服务端插件负责真实文件读写、下载代理、资源索引和打开目录。** 两者组合后，才能做到“普通用户安装一次，资源长期保留”。

## 2. 当前架构

```text
浏览器前端扩展
  ├─ 扫描角色卡 / 聊天消息 / iframe 中的在线 URL
  ├─ 显示悬浮球、估算大小、下载队列、资源管理界面
  ├─ 调用 /api/plugins/frontend-media-localizer/*
  └─ 将已下载 URL 改写为本地资源 URL
                 │
                 ▼
酒馆服务端插件
  ├─ 校验远程 URL 与下载类型
  ├─ 下载并落盘到 data/resources/角色卡名/
  ├─ 写入 manifest.json 资源索引
  ├─ 提供文件读取、删除、卡片列表、打开目录接口
  └─ 返回本地路由供前端使用
                 │
                 ▼
data/resources/角色卡名/
  ├─ 媒体文件
  └─ manifest.json
```

前端扩展目录：

```text
public/scripts/extensions/third-party/frontend-media-localizer/
```

服务端插件目录：

```text
plugins/frontend-media-localizer/
```

资源目录使用相对酒馆 `data` 的位置，不绑定盘符：

```text
data/resources/角色卡名/
```

## 3. 服务端插件制作经验

### 3.1 服务端插件是“受控文件系统能力”

SillyTavern 服务端插件通过 `plugins/` 目录加载，并把路由注册在：

```text
/api/plugins/<插件 ID>/
```

本项目的 ID 为 `frontend-media-localizer`。前端统一通过这个 API 地址访问后端，不直接接触系统真实路径。

这层隔离很重要：浏览器端只知道“下载、查看、删除、打开”这些业务动作；真正的磁盘路径、文件名和操作系统命令都留在服务端控制。

### 3.2 资源索引不要只靠文件名

每张角色卡目录下维护 `manifest.json`，记录：原始 URL、资源 ID、媒体类型、大小、内容类型、本地文件名、下载状态等。

优点：

- 原始 URL 可以稳定映射到本地路由，前端无需猜测文件名。
- 能显示下载数量、分类统计和资源大小。
- 删除时可以同时删除文件和索引项，避免脏数据。
- 图床 URL 相同、文件名相同或 URL 中没有扩展名时，仍可用哈希 ID 管理。

写入索引时使用临时文件后再重命名，避免酒馆意外退出时留下半截 JSON。

### 3.3 必须做路径与网络安全处理

服务端下载器不是简单 `fetch(url)`。本项目已加入以下边界：

- 仅允许 `http` 与 `https`。
- 拒绝 URL 中的用户名与密码。
- 对卡名使用安全文件名清理，避免 `../` 等路径穿越。
- 使用 SHA-256 生成资源 ID，避免直接以远程文件名落盘。
- 限制下载体积、重定向次数与探测超时。
- 对解析到的域名/IP 做私网与回环地址限制，避免前端卡借下载器访问局域网、路由器或酒馆本机服务（SSRF）。
- 文件读取接口只按索引返回当前角色卡目录内的文件。

这些措施是把“能下载”变成“适合公开给其他用户安装”的关键。

### 3.4 打开文件夹必须由服务端执行

“打开对应资源文件夹”不能由网页直接完成。服务端根据当前操作系统调用 `explorer.exe`、`open` 或 `xdg-open`，并且先确保目标目录存在。

Windows 下实践中要使用 `spawn` 启动 `explorer.exe`，而不是把路径拼成 shell 命令；这样能减少带空格、中文或特殊字符路径导致的失败，也避免命令注入风险。

### 3.5 前端自安装的可行边界

酒馆的“安装扩展程序”只会把 Git 仓库克隆到 `public/scripts/extensions/third-party`，不会自动安装 `plugins/` 下的服务端代码。

本项目的折中方案是：前端扩展附带 `server-plugin/index.mjs` 与 `package.json` 模板。检测不到服务端时，弹出安装窗口；用户通过浏览器目录句柄明确选择 `SillyTavern/plugins` 后，前端把模板写入 `plugins/frontend-media-localizer`。

这不是静默安装：用户必须主动选目录，安装后还必须重启酒馆，并在 `config.yaml` 中开启：

```yaml
enableServerPlugins: true
```

这是浏览器安全模型和 SillyTavern 插件加载机制决定的合理边界。

## 4. 前端扩展与服务端插件对比

| 维度 | 前端扩展 | 服务端插件 |
| --- | --- | --- |
| 运行位置 | 浏览器页面 | SillyTavern 的 Node.js 进程 |
| 最适合的工作 | UI、悬浮球、扫描、进度、队列、文本/DOM/iframe 改写 | 下载、磁盘读写、资源索引、文件服务、删除、打开目录 |
| 文件系统能力 | 默认没有；需用户每次/一次性句柄授权 | 可管理酒馆 `data` 和插件自身授权范围内的目录 |
| 是否能自动存入 `data/resources` | 不能稳定做到 | 可以 |
| 是否能打开系统文件夹 | 不能 | 可以 |
| 是否需要重启酒馆 | 前端通常刷新即可 | 安装、升级或修改后需要重启以加载 |
| 是否容易通过酒馆 Git 窗口安装 | 是 | 否，原生安装窗口不处理 `plugins/` |
| 安全风险 | 主要是页面权限、跨域和用户句柄 | 有服务端文件/网络权限，必须严格验证输入 |
| 离线可靠性 | 页面缓存不等于文件备份 | 本地媒体文件真实存在，可随 `data` 备份迁移 |

## 5. 各自优劣

### 前端扩展优势

- 用户可见、交互快，适合把复杂过程做成扫描窗口和悬浮球。
- 可直接观察当前角色卡、聊天 DOM 和 iframe 资源。
- 可在下载完成后即时改写资源 URL，无需刷新整张卡。
- 通过酒馆的 Git URL 窗口安装方便，适合发布和更新。

### 前端扩展限制

- 浏览器不能无授权写入 `SillyTavern/data` 或执行系统命令。
- 直接跨域下载会遇到 CORS、防盗链和浏览器安全限制。
- IndexedDB 适合作为临时缓存，不适合当作可见、可备份、可迁移的媒体仓库。
- 单靠前端无法安全保证文件名、目录隔离、下载体积限制和局域网访问防护。

### 服务端插件优势

- 下载结果是真正的文件，可备份、复制、迁移并离线使用。
- 不受浏览器 CORS 直接限制，能以服务端方式处理重定向、大小探测和流式下载。
- 可以提供统一的本地文件路由，让前端卡继续使用普通 URL。
- 可以集中做输入校验、路径隔离、媒体类型限制和删除管理。

### 服务端插件限制

- 安装门槛更高：需要 `plugins/` 目录、`enableServerPlugins` 配置和重启。
- 代码一旦有漏洞，影响范围比纯前端更大，必须审计网络和文件操作。
- 不能像前端一样直接读取页面 DOM，仍然需要前端提供“发现了哪些资源”。

## 6. 可复用的开发建议

1. **按职责拆分。** 前端只负责“发现、选择、展示和替换”，服务端只负责“验证、下载、存储和提供文件”。
2. **所有服务端 API 以业务动作命名。** 例如 `/probe`、`/download`、`/library`、`/delete`、`/open`，不要暴露任意路径读写接口。
3. **默认相对 `DATA_ROOT`。** 不把 `F:`、用户名或机器目录写死在代码中，保证其他用户安装后可直接使用。
4. **索引优先于扫描目录。** manifest 是前端显示状态、路由替换、删除资源和统计容量的单一可信来源。
5. **失败应保留。** 不伪造“全部成功”；保留失败条目与错误原因，方便用户在图床恢复后重试。
6. **把下载做成队列。** 多张卡并行触发时，前端使用统一队列并显示当前任务，避免重复下载和进度混乱。
7. **安装流程必须清楚。** 前端自安装仅能在用户授权目录后写模板；要明确提示目标目录、重启要求和服务端插件开关。
8. **发布时前后端版本要同步。** 前端扩展附带的 `server-plugin` 模板应与仓库中服务端实际源码保持一致；升级 API 时优先考虑兼容旧 manifest。

## 7. 对其他 AI 的简短提示词

若要继续开发此类插件，可向 AI 提供以下前提：

> 这是一个 SillyTavern 双端扩展：`public/scripts/extensions/third-party/frontend-media-localizer` 负责浏览器 UI、资源扫描、悬浮球、队列和 URL 改写；`plugins/frontend-media-localizer` 负责安全下载、`data/resources/<卡名>` 文件存储、manifest 索引、本地文件路由、删除与打开目录。不得将绝对盘符写死；服务端接口必须防止路径穿越、SSRF、超大文件和任意文件读写。前端不可直接写 `data`，只能通过 API 或用户明确授权的目录句柄安装后端模板。

---

本文依据当前项目实现编写，适合作为同类 SillyTavern 服务端扩展的设计与审计参考。
